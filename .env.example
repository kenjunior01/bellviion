# Local SQLite database (Prisma)
DATABASE_URL="file:./db/custom.db"

# PayPal (optional in demo mode — works without these)
# Get credentials at https://developer.paypal.com/dashboard/applications
PAYPAL_CLIENT_ID=""
PAYPAL_CLIENT_SECRET=""
PAYPAL_MODE="sandbox" # "live" for production
# Note: you can also configure PayPal inside Admin → Settings (stored in DB)
